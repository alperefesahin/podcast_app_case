import 'package:flutter/material.dart';

const introductionScreenButtonsPadding = EdgeInsets.symmetric(vertical: 15, horizontal: 30);
const controlsPadding = EdgeInsets.only(top: 10, left: 12, right: 12);
